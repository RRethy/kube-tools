apiVersion: apps/v1
kind: Deployment
metadata:
  name: valid-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: valid
  template:
    metadata:
      labels:
        app: valid
    spec:
      containers:
      - name: app
        image: nginx:1.21